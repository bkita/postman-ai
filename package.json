{
  "name": "postman-api-ai",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "pm:export:col": "dotenv node scripts/exportPostmanCollection.js",
    "pm:export:env": "dotenv node scripts/exportPostmanEnvironment.js",
    "pm:sync": "npm run pm:export:col && npm run pm:export:env",
    "pm:test": "dotenv -- node scripts/runNewman.js",
    "pm:sync:test": "npm run pm:sync && npm run pm:test"
  },
  "dependencies": {
    "dotenv-cli": "^11.0.0"
  },
  "devDependencies": {
    "newman": "^6.2.1",
    "newman-reporter-htmlextra": "^1.23.1"
  }
}
